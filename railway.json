{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python healthcheck.py && cd /app && PYTHONPATH=/app uvicorn api.main:app --host 0.0.0.0 --port $PORT"
  }
}
