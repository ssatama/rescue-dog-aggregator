# === Database Configuration ===
DB_HOST=localhost
DB_NAME=rescue_dogs
DB_USER=your_username
DB_PASSWORD=your_password_if_any

# === CORS Security Configuration ===
# Comma-separated list of allowed origins for CORS requests
# Development: Include all local development URLs
# Production: Only include your actual frontend domain(s)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Whether to allow credentials (cookies, auth headers) in CORS requests
# Set to true only if you need authentication across origins
CORS_ALLOW_CREDENTIALS=false

# Application environment (development/staging/production)
# Controls CORS strictness and security defaults
ENVIRONMENT=development

# === Cloudinary Configuration ===
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# === Testing ===
TESTING=false

# Frontend Cloudinary Configuration
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
