[pytest]
testpaths = tests
asyncio_mode = strict
# Explicitly set the default loop scope to function to silence the warning
# and align with future pytest-asyncio behavior.
asyncio_default_fixture_loop_scope = function